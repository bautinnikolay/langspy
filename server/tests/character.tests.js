const expect = require('expect')
const request = require('supertest')

const {app} = require('./../server')

describe('tests for characters methods', () => {
  it('should return new character first name and last name', (done) => {

  })

  it('should return 403 response without new character info', (done) => {

  })

  it('should save character to database', (done) => {

  })

  it('should return error without saving character to database', (done) => {

  })
})
